{
  "name": "grid",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "scss": "node-sass src --source-map true --output-style nested -o dist",
    "autoprefixer": "postcss -u autoprefixer -r dist/*.css --map",
    "build": "npm run scss && npm run autoprefixer"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^9.7.3",
    "node-sass": "^7.0.0",
    "postcss-cli": "^6.1.3"
  }
}
